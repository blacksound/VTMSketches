a = VTMMKtlDevice.new( \myTest, ( mktlName: \myLPD8, mktlDescription: "akai-lpd8" ), definition: ( ) );

a.returns
a.attributes
returnDescriptions





// Is there a signal for when a connection is made?

// queries, attributes:
// - not an Array, since we want to access by name...
// - so how do we do that? or are they changed into VTMOrderedIdentityDictionary?

